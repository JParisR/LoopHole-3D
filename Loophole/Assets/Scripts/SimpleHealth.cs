using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SimpleHealth : MonoBehaviour
{
    /*
    public Slider healthBar;
    SpaceshipBehaviour playerHealth;

    void Start(){
        playerHealth = GameObject.FindWithTag("Player Spaceship").GetComponent<SpaceshipBehaviour>();
    }
    void Update(){
        if (healthBar.value != playerHealth.health)
        {
            Debug.Log("cambiamos health");
        }    
        healthBar.value = playerHealth.health;
    }
    */
}
